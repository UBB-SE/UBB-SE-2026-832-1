
    
<Page
    x:Class="WinUI.Views.Reminders.RemindersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ClassLibrary.DTOs"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <TextBlock Text="Nutrition Assistant" FontSize="16" Foreground="Gray" VerticalAlignment="Center" />
                <Button
                    Content="Add New Reminder"
                    Style="{StaticResource AccentButtonStyle}"
                    Margin="16,0,0,0"
                    HorizontalAlignment="Right" />
            </StackPanel>
            <TextBlock Text="Reminders" FontSize="32" FontWeight="Bold" />
        </StackPanel>

        <Border
            Grid.Row="1"
            Background="{ThemeResource AccentFillColorDefaultBrush}"
            CornerRadius="12"
            Padding="28,24"
            Margin="0,0,0,24"
            Visibility="{x:Bind ViewModel.NextReminder, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
            <Grid ColumnDefinitions="Auto,*">
                <Border
                    Grid.Column="0"
                    Width="52"
                    Height="52"
                    CornerRadius="26"
                    Background="White"
                    Opacity="0.15"
                    VerticalAlignment="Center"
                    Margin="0,0,16,0">
                    <FontIcon
                        Glyph="&#xE823;"
                        FontSize="28"
                        Foreground="White"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                    <TextBlock
                        Text="Next Active Reminder"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="White"
                        Opacity="0.75" />
                    <TextBlock
                        Text="{x:Bind ViewModel.NextReminder.Name, Mode=OneWay}"
                        Style="{StaticResource TitleLargeTextBlockStyle}"
                        Foreground="White" />
                    <TextBlock
                        Text="{x:Bind ViewModel.NextReminder.FullDateTimeDisplay, Mode=OneWay}"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Foreground="White"
                        Opacity="0.85" />
                </StackPanel>
            </Grid>
        </Border>

        <ListView
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.Reminders, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ReminderDto">
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16,12"
                        Margin="0,4,0,8">
                        <Grid ColumnDefinitions="*,Auto">
                            <StackPanel Grid.Column="0" Spacing="2">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="{x:Bind FullDateTimeDisplay, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xE8EF;" FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                                    <TextBlock Text="{x:Bind Frequency, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Button
                                    Style="{StaticResource DefaultButtonStyle}"
                                    Padding="12,4"
                                    Content="Edit" />
                                <Button
                                    Background="Transparent"
                                    BorderThickness="0"
                                    ToolTipService.ToolTip="Delete Reminder">
                                    <SymbolIcon Symbol="Cancel" Foreground="Red" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <ProgressRing
            Grid.Row="2"
            IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center" />
    </Grid>
</Page>
