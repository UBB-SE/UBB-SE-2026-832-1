<Page
    x:Class="WinUI.Views.DailyLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Daily Nutrition Log" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <TextBlock
            Grid.Row="1"
            Text="{Binding ErrorMessage}"
            Foreground="Red"
            TextWrapping="Wrap"
            Margin="0,0,0,8"/>

        <TextBlock
            Grid.Row="2"
            Text="{Binding StatusMessage}"
            Foreground="Green"
            TextWrapping="Wrap"
            Margin="0,0,0,8"/>

        <!-- Today's macro summary -->
        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                <TextBlock Text="Calories" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TodayTotals.TotalCalories}" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock Text="Protein (g)" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TodayTotals.TotalProtein}" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                <TextBlock Text="Carbs (g)" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TodayTotals.TotalCarbohydrates}" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                <TextBlock Text="Fat (g)" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TodayTotals.TotalFat}" Style="{ThemeResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Food search and results -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    x:Name="SearchBox"
                    PlaceholderText="Search food items..."
                    Margin="0,0,8,0"/>

                <Button
                    Grid.Column="1"
                    Content="Search"
                    x:Name="SearchButton"
                    Style="{ThemeResource AccentButtonStyle}"/>
            </Grid>

            <ListView
                Grid.Row="1"
                x:Name="FoodResultsListView"
                ItemsSource="{Binding FoodSearchResults}"
                SelectedItem="{Binding SelectedFoodItem, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="SemiBold"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Cal" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Calories}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Protein" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Protein}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3">
                                <TextBlock Text="Carbs" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Carbohydrates}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Fat" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Fat}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Button
            Grid.Row="5"
            Content="Log Selected Food"
            x:Name="LogFoodButton"
            Style="{ThemeResource AccentButtonStyle}"
            HorizontalAlignment="Stretch"
            Margin="0,8,0,0"/>

        <!-- Loading overlay -->
        <ProgressRing
            Grid.Row="0"
            Grid.RowSpan="6"
            IsActive="{Binding IsLoading}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="48"
            Height="48"/>
    </Grid>
</Page>
