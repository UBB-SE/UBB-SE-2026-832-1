<Page
    x:Class="WinUI.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <TabView 
            x:Name="mainTabView"
            TabWidthMode="Equal"
            CanDragTabs="True"
            CanReorderTabs="True"
            IsAddTabButtonVisible="False"
            TabItemsSource="{x:Bind ViewModel.Tabs, Mode=OneWay}"
            SelectedIndex="{x:Bind ViewModel.SelectedTabIndex, Mode=TwoWay}">

            <TabView.TabItemTemplate>
                <DataTemplate>
                    <TabViewItem Header="{Binding Title}" IsClosable="False">
                        <Frame Content="{Binding Content}" />
                    </TabViewItem>
                </DataTemplate>
            </TabView.TabItemTemplate>

        </TabView>
    </Grid>
</Page>
