<Page
    x:Class="WinUI.Views.InventoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Inventory" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <!-- Messages -->
        <StackPanel Grid.Row="1" Spacing="4" Margin="0,0,0,8">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Green" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Add ingredient by selection -->
        <StackPanel Grid.Row="2" Spacing="8" Margin="0,0,0,16" MaxWidth="600">
            <TextBlock Text="Add Ingredient to Pantry" Style="{ThemeResource SubtitleTextBlockStyle}"/>

            <ComboBox
                Header="Select Ingredient"
                PlaceholderText="Choose an ingredient"
                HorizontalAlignment="Stretch"
                ItemsSource="{Binding AvailableIngredients}"
                SelectedItem="{Binding SelectedIngredient, Mode=TwoWay}"
                DisplayMemberPath="Name"/>

            <NumberBox
                Header="Quantity (grams)"
                Minimum="1"
                SmallChange="1"
                HorizontalAlignment="Stretch"
                Value="{Binding QuantityGramsInput, Mode=TwoWay}"/>

            <Button
                x:Name="AddBySelectionButton"
                Content="Add Selected Ingredient"
                Style="{ThemeResource AccentButtonStyle}"
                HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Add ingredient by name -->
        <StackPanel Grid.Row="3" Spacing="8" Margin="0,0,0,16" MaxWidth="600">
            <TextBlock Text="Add Ingredient by Name" Style="{ThemeResource SubtitleTextBlockStyle}"/>

            <TextBox
                Header="Ingredient Name"
                PlaceholderText="Enter ingredient name"
                HorizontalAlignment="Stretch"
                Text="{Binding IngredientNameInput, Mode=TwoWay}"/>

            <Button
                x:Name="AddByNameButton"
                Content="Add by Name"
                HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Inventory list -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Current Inventory" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>

            <ListView
                Grid.Row="1"
                ItemsSource="{Binding InventoryItems}"
                SelectedItem="{Binding SelectedInventoryItem, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <TextBlock Text="{Binding IngredientName}" MinWidth="160"/>
                            <TextBlock>
                                <Run Text="{Binding QuantityGrams}"/>
                                <Run Text=" g"/>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button
                Grid.Row="2"
                x:Name="RemoveItemButton"
                Content="Remove Selected Item"
                Margin="0,8,0,0"
                HorizontalAlignment="Left"/>
        </Grid>

        <!-- Loading overlay -->
        <ProgressRing
            Grid.Row="0"
            Grid.RowSpan="5"
            IsActive="{Binding IsLoading}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="48"
            Height="48"/>
    </Grid>
</Page>
